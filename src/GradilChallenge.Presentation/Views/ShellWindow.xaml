<Window x:Class="GradilChallenge.Presentation.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculadora de Gradil" Height="550" Width="1200"
        WindowStartupLocation="CenterScreen">

    <DockPanel Margin="16">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Calculadora de Gradil" FontSize="20" FontWeight="Bold"/>

            <Button Grid.Column="1" Content="Ver Histórico de Pedidos"
                    Command="{Binding ShowHistoryCommand}"
                    Padding="10,4"
                    Visibility="{Binding IsHistoryVisible, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

            <Button Grid.Column="1" Content="Nova Cotação"
                    Command="{Binding ShowNewQuoteCommand}"
                    Padding="10,4"
                    Visibility="{Binding IsHistoryVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>

        <ContentControl Content="{Binding CurrentViewModel}"/>
    </DockPanel>
</Window>
